CREATE TABLE Users (
    USER_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    USERNAME VARCHAR2(50) NOT NULL UNIQUE,
    PASSWORD VARCHAR2(255) NOT NULL,
    EMAIL VARCHAR2(100) NOT NULL UNIQUE,
    LOCATION VARCHAR2(100),
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (USER_ID)
);

CREATE TABLE Jobs (
    JOB_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    TYPE VARCHAR2(10) CHECK (TYPE IN ('구인', '구직')) NOT NULL, /* 구인 또는 구직 구분 */
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (JOB_ID)
);

ALTER TABLE Jobs ADD CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP;

ALTER TABLE Jobs ADD USERNAME VARCHAR2(50);

-- 초급 매치 테이블
CREATE TABLE BeginnerMatches (
    MATCH_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    USERNAME VARCHAR2(50),
    PRIMARY KEY (MATCH_ID)
);

-- 중급 매치 테이블
CREATE TABLE MiddleClassMatches (
    MATCH_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    USERNAME VARCHAR2(50),
    PRIMARY KEY (MATCH_ID)
);

-- 상급 매치 테이블
CREATE TABLE AdvancedMatches (
    MATCH_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    USERNAME VARCHAR2(50),
    PRIMARY KEY (MATCH_ID)
);

-- 축구 매치 테이블
CREATE TABLE soccer (
    MATCH_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    USERNAME VARCHAR2(50),
    PRIMARY KEY (MATCH_ID)
);

-- 4vs4 매치 테이블
CREATE TABLE FourVsFourMatches (
    MATCH_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    USERNAME VARCHAR2(50),
    PRIMARY KEY (MATCH_ID)
);

-- 실내 매치 테이블
CREATE TABLE IndoorMatches (
    MATCH_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    USERNAME VARCHAR2(50),
    PRIMARY KEY (MATCH_ID)
);

-- 야외 매치 테이블
CREATE TABLE OutdoorMatches (
    MATCH_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TITLE VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CONTACT VARCHAR2(50) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    USERNAME VARCHAR2(50),
    PRIMARY KEY (MATCH_ID)
);

CREATE TABLE Teams (
    TEAM_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TEAM_NAME VARCHAR2(100) NOT NULL,
    DESCRIPTION CLOB,
    TYPE VARCHAR2(10) CHECK (TYPE IN ('풋살', '축구')) NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (TEAM_ID)
);

-- 풋살팀 데이터 삽입
INSERT INTO Teams (TEAM_NAME, DESCRIPTION, TYPE) VALUES ('풋살팀 A', '풋살팀 A 설명', '풋살');
INSERT INTO Teams (TEAM_NAME, DESCRIPTION, TYPE) VALUES ('풋살팀 B', '풋살팀 B 설명', '풋살');
INSERT INTO Teams (TEAM_NAME, DESCRIPTION, TYPE) VALUES ('풋살팀 C', '풋살팀 C 설명', '풋살');

-- 축구팀 데이터 삽입
INSERT INTO Teams (TEAM_NAME, DESCRIPTION, TYPE) VALUES ('축구팀 A', '축구팀 A 설명', '축구');
INSERT INTO Teams (TEAM_NAME, DESCRIPTION, TYPE) VALUES ('축구팀 B', '축구팀 B 설명', '축구');
INSERT INTO Teams (TEAM_NAME, DESCRIPTION, TYPE) VALUES ('축구팀 C', '축구팀 C 설명', '축구');

CREATE TABLE GuestRecruitments (
    RECRUITMENT_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TYPE VARCHAR2(50) NOT NULL,
    REGION VARCHAR2(100) NOT NULL,
    DESCRIPTION CLOB NOT NULL,
    CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (RECRUITMENT_ID)
);

CREATE TABLE TeamMembers (
    MEMBER_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    TEAM_NAME VARCHAR2(255) NOT NULL,
    MEMBER_NAME VARCHAR2(100) NOT NULL,
    POSITION VARCHAR2(50),
    JOINED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (MEMBER_ID)
);

